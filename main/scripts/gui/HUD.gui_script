local monarch = require("monarch.monarch")

function init(self)
	
	self.doors_selected = false
	self.rooms_selected = false
	msg.post(".", "acquire_input_focus")
end

function final(self)
	-- Add finalization code here
	-- Remove this function if not needed
end

function update(self, dt)
	if not self.doors_selected and map_state.doors_selected() then
		self.doors_selected = true
		select_doors(true)
	elseif self.doors_selected and not map_state.doors_selected() then
		self.doors_selected = false
		select_doors(false)
	end

	if not self.rooms_selected and map_state.rooms_selected() then
		self.rooms_selected = true
		select_rooms(true)
	elseif self.rooms_selected and not map_state.rooms_selected() then
		self.rooms_selected = false
		select_rooms(false)
	end
end

function on_message(self, message_id, message, sender)
end

function on_input(self, action_id, action)
	
end

function select_doors(selected)
	if selected then
		local node = gui.get_node("rooms")
		gui.set_color(node, vmath.vector3(0.5, 0.5, 0.5))
	else
		local node = gui.get_node("rooms")
		gui.set_color(node, vmath.vector3(1, 1, 1))
	end
end

function select_rooms(selected)
	if selected then
		local node = gui.get_node("doors")
		gui.set_color(node, vmath.vector3(0.5, 0.5, 0.5))
	else
		local node = gui.get_node("doors")
		gui.set_color(node, vmath.vector3(1, 1, 1))
	end
end


function on_reload(self)
	-- Add input-handling code here
	-- Remove this function if not needed
end
